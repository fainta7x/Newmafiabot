# Server configuration
PORT=3000
NODE_ENV=development

# Organizer Authentication
ORGANIZER_PASSWORD=admin
JWT_SECRET=super-secret-jwt-key-change-this-in-production

# SQLite Database path
DATABASE_PATH=./mafia_club.db
SEED_DEMO_DATA=false

# Legacy Telegram Bot Token (Must be revoked in BotFather if previously leaked)
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_URL=
ORGANIZER_CHAT_ID=
# Integration settings
BOT_API_BASE_URL=http://127.0.0.1:3000
BOT_API_SECRET=

# VK publishing and poll sync for 2LA Noire.
# Non-secret club references are also built into the adapter; these vars can override them if VK IDs/URLs change.
VK_GROUP_ID=233806277
VK_GROUP_SCREEN_NAME=2lanoiremafia
VK_GROUP_URL=https://vk.ru/2lanoiremafia
VK_CHANNEL_PEER_ID=-233806277
VK_CHANNEL_URL=https://vk.ru/im/channels/-233806277
# User token: required for polls.create / polls.getVoters and used for public-wall operations.
VK_ACCESS_TOKEN=
# Community token: preferred for Messenger group-channel operations (messages.getConversationsById/send/edit).
# If omitted, the adapter can fall back to VK_ACCESS_TOKEN for compatibility.
VK_GROUP_ACCESS_TOKEN=
VK_API_VERSION=5.199
# Legacy override: positive community ID is converted to a negative channel peer_id.
VK_CHANNEL_GROUP_ID=
# Configure the same secret in VK community Callback API settings.
VK_CALLBACK_SECRET=
# Confirmation string is issued by VK for the community. Public + channel currently use the same community ID.
VK_CALLBACK_CONFIRMATION=
VK_CHANNEL_CALLBACK_CONFIRMATION=
