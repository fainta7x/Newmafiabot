name: ChatGPT live flow patch

on:
  push:
    branches: [chatgpt-write-test]
    paths:
      - scripts/chatgpt_apply_live_flow_patch.py

permissions:
  contents: write

jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt-write-test
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Apply targeted patch
        run: python3 scripts/chatgpt_apply_live_flow_patch.py
      - name: Install
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Tests
        run: npm test
      - name: Remove one-off patch machinery
        run: rm -f scripts/chatgpt_apply_live_flow_patch.py .github/workflows/chatgpt-live-flow-patch.yml
      - name: Commit verified patch
        run: |
          git config user.name "ChatGPT Patch Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "fix(live-game): farewell queue, auto winner and global undo"
          git push origin HEAD:chatgpt-write-test
